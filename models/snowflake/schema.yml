version: 2

models:
  - name: details
    tests:
      - dbt_utils.equality:
          compare_model: ref('details_expected')
          compare_columns:
            - name
            - id
            - city

sources:
  - name: snowflake
    database: dbt_test
    schema: raw
    tables: 
      - name: customer
      - name: contact

  - name: test_snowflake
    database: dbt_test
    schema: unit_testing
    tables:
      - name: test_contact
      - name: test_customer